package com.explead.seasons.winter.interfaces

interface OnMoveCubeListener {
    fun onUp(x: Int, y: Int)
    fun onDown(x: Int, y: Int)
    fun onRight(x: Int, y: Int)
    fun onLeft(x: Int, y: Int)
}
